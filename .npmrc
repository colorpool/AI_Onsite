# 忽略peer dependency警告
legacy-peer-deps=true

# 强制解析peer dependencies
strict-peer-dependencies=false

# 使用npm而不是yarn
package-manager=npm

# 设置registry
registry=https://registry.npmjs.org/

# 减少警告输出
loglevel=error
